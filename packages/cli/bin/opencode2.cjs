#!/usr/bin/env node

console.log(
  '"opencode2" has been renamed to "opencode". Run "opencode" instead.\n\nIf you have OpenCode v1 installed, you may need to uninstall it so the "opencode" command launches the correct installation.',
)
process.exit(1)
