import type { DesktopExtension } from "../extension"

// Replaced at build time only when OPENCODE_DESKTOP_EXTENSION is supplied.
const extension: DesktopExtension = { apiVersion: 1 }
export default extension
